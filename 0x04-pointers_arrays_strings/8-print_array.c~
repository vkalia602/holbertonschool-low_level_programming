#include <stdio.h>
#include "holberton.h"
/**
 * print_line - printing underscore n number of times
 * @a - 
 * Return: void
 */
void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; i++)
	{
		printf("%d", a[i]);
		if (i == (n - 1))
		{
			break;
		}
		printf(", ");
	}
	printf("\n");
}
