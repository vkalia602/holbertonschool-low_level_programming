#include "holberton.h"
/**
 * _islower - function checks for the lowercase character
 *
 * Return: prints a value of 1 if lowercase, else 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
